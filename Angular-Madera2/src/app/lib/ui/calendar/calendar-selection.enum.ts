/**
 * Type de sélection possible, pour l'instant aucune
 * mais à terme on pourra surement sélectionner
 * une date, plusieurs, une période, etc
 */
export enum CalendarSelectionEnum {
    none = 'none'
}
