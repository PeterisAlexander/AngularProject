/**
 * Interface décrivant la personnalisation des styles possibles
 * sur une cellule (un jour en affichage mois)
 */
export interface CalendarCellStyle {
    backgroundColor?: string;
    borderColor?: string;
    isBold?: boolean;
    textColor?: string;
}
